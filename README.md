# Product Card UI with React Props

This project demonstrates how React components pass data with props and render dynamic content using reusable UI pieces.

## Features

- Parent-to-child data flow with props
- Reusable `Card`, `Header`, `Button`, and `Footer` components
- Dynamic product rendering from an array of objects
- One-way data flow using props and state in the parent component